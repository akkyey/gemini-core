---
description: Safe-Shell V2
---


> [!IMPORTANT]
> **⚠️ [Safety Interlock]**
> 本手順を開始する前に、必ず `.agent/project_config.md` を読み込み、`SAFETY_SHELL_TOOL` 等の論理名を物理的な実体（Path/Tool ID）へ解決せよ。規約を無視した直接的な `run_command` 等の使用はプロセス違反となる。
5 Safe-Shell Execve / Isolation / Persistence

## 
-  **5** 
- `PYTHONPATH` 
-  I/O 

## 

### 1. 
1. `ls` 

### 2.  (Short Task)   (Long Task)
2. 
   - **5**: `execute_safe` 
   - **5**: `register_macro` `background=True`  `execute_macro` 

### 3. 
3.  `get_process_status` 

## 
- ****:  `export` Safe-Shell 
